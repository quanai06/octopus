# Compute Budget - {{ project_name or "Unnamed Project" }}

> Generated by Octopus CLI v{{ octopus_version }} on {{ generated_at }}

---

## Resource Profile

- GPU available: {{ "yes" if compute.has_gpu else "no" }}
{% if compute.environment %}
- Environment: {{ compute.environment }}
{% endif %}
{% if compute.deadline %}
- Deadline: {{ compute.deadline }}
{% endif %}
{% if compute.budget_note %}
- Constraint note: {{ compute.budget_note }}
{% endif %}

## Recommended Budget

{% for note in compute_budget_notes %}
- {{ note }}
{% endfor %}

## Training Limits

{% if compute.has_gpu %}
- Start with batch size 16 for transformer text classification, then adjust by memory.
- Start with 3 epochs before longer training.
- Run at most one hyperparameter sweep after the baseline comparison passes.
{% else %}
- Prefer CPU-friendly baselines first.
- Use small sampled smoke tests for pipeline validation.
- Avoid full neural fine-tuning until GPU access is available.
{% endif %}

## Metrics to Log

- Wall-clock train time.
- Peak memory if available.
- Inference latency per sample.
- Main metric: {{ main_metric }}
{% for metric in secondary_metrics %}
- Secondary metric: {{ metric }}
{% endfor %}

## Compute Risk

- Stop runs that do not improve validation {{ main_metric }} after controlled changes.
- Avoid changing model, data split, preprocessing, and class weighting in the same run.
- Keep failed runs in the experiment log.
