# Data Strategy - {{ project_name or "Unnamed Project" }}

> Generated by Octopus CLI v{{ octopus_version }} on {{ generated_at }}

---

## Dataset Readiness

- Status: {{ dataset_status or "unknown" }}
- Input type: {{ input_type or "to be defined" }}
- Output type: {{ output_type or "to be defined" }}
{% if dataset_size_note %}
- Size note: {{ dataset_size_note }}
{% endif %}
{% if has_labels is not none %}
- Labels: {{ "available" if has_labels else "not available" }}
{% endif %}
{% if language %}
- Language: {{ language }}
{% endif %}

## Split & Cross-Validation

The baseline is a full train/eval protocol, not a single random holdout:

{% for step in evaluation_protocol %}
- {{ step }}
{% endfor %}
- Store the CV scheme, fold seed, and split file paths in experiment logs.

## Quality Checks

{% if data_checks %}
{% for check in data_checks %}
- {{ check }}
{% endfor %}
{% else %}
- Confirm dataset source and ownership.
- Check duplicate samples across splits.
- Review label quality on a random sample.
{% endif %}

## Leakage Checks

- Check whether target labels or future information appear in input features.
- Check duplicate or near-duplicate samples across train / validation / test.
- Keep preprocessing fit only on train data.

## Imbalance Handling

{% if has_class_imbalance %}
- Class imbalance is expected. Track per-class recall and confusion matrix.
- Try class weights or balanced sampling after the first clean baseline.
{% else %}
- No known class imbalance yet. Still inspect label distribution before training.
{% endif %}

## Data Decision Gate

Do not run advanced model training until dataset split, labels, leakage checks, and baseline metric are documented.
