# Experiment Plan - {{ project_name or "Unnamed Project" }}

> Generated by Octopus CLI v{{ octopus_version }} on {{ generated_at }}

---

## Goal

Establish a reproducible baseline before exploring advanced models.

## Baseline Experiment

| Field | Value |
|---|---|
| Model | {{ baseline_models[0] }} |
| Metric | {{ main_metric or metrics[0] }} |
| Dataset | Full train set with stratified split |
| Status | Not started |

## Experiment Queue

| ID | Model | Priority | Depends On | Status |
|---|---|---|---|---|
| E001 | {{ baseline_models[0] }} | High | - | Todo |
{% if baseline_models | length > 1 %}
| E002 | {{ baseline_models[1] }} | High | E001 | Todo |
{% endif %}
| E003 | _Main model TBD_ | Medium | E001 | Todo |

## Metrics to Track

{% for metric in metrics %}
- {{ metric }}
{% endfor %}
- Training time (seconds)
- Inference time per sample (ms)

## Logging Format

```json
{
  "experiment_id": "E001",
  "model": "{{ baseline_models[0] }}",
  "{{ main_metric or metrics[0] }}": null,
  "train_time_sec": null,
  "notes": ""
}
```

## Stop Condition

- Reached target score: {{ target_score or "not set" }}
- Or exhausted compute budget

## Next Decision Rules

- If baseline {{ main_metric or metrics[0] }} is below expected, check data quality first.
- If baseline {{ main_metric or metrics[0] }} meets expectations, move to E003.
- Always document why an experiment was stopped or skipped.
